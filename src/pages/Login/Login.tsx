import { LoginHeader } from 'components/login-header';
import { LoginMain } from 'components/login-main';

export const Login = () => {
    return (
        <>
            <LoginHeader />
            <LoginMain />
        </>
    );
};
